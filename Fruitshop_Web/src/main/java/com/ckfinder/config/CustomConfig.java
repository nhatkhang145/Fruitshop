package com.ckfinder.config; // Nhớ sửa package cho đúng với thư mục bạn đặt

import com.cksource.ckfinder.config.Config;
import com.cksource.ckfinder.config.loader.ConfigLoader;

public class CustomConfig extends Config {

    public CustomConfig() throws Exception {
        // Cấu hình mặc định
        // Bạn có thể set license, đường dẫn baseDir ở đây nếu muốn code cứng
    }


}